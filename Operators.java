class Main {
  public static void main(String[] args) {
    //This is how you declare and print strings
    String name = "John";
    System.out.println ("Hello" + "fd "+ name);
    int bigNum = 100;
    bigNum = 10; // bigNum is now 10
    System.out.println(bigNum);

   // final int bigNum = 7;
   // bigNum = 20; v
// Here we put variables into a variable
   String firstName = "John";
   String lastName = "Doe";
   String fullName = firstName + lastName;
   System.out.println(fullName);

// Now let's make calculations
int x = 5, y = 6, z = 50;
System.out.println (x+y+z);
System.out.println (x*y*z);

// Java Identifiers
int minutesPerHour = 60; //This is good
int m = 60; //This is bad 

//Java Data Type
 int myNum = 5; //Integer (Whole Numbers)
 float myFloatNum = 5.99f; //Floating point Numbers
 char myLetter = 'D';  // Character
 String myText = "Hello"; //String
 boolean myBool = true; //boolean

//Java operation
 int a = 100 + 50;
 int sum1 = 100 + 500; //600
 int sum2 = sum1 + 250;
 int sum3 = sum2 + sum2;
 //Flores Flores is having a PBA point

 /* Arithmetic Operators*/
 // + Addition (x+y)
 // - Substraction (x-y)
 //* Multiplication (x*y)
 // / Division (x/y)
 // % Modulus(Return the division remainder) (x%y)
 // Andrew is getting a PBA
 // ++ Increment (Increment the value of a variable by 1) (++x)
 // -- Decrement (Decrement the value of a variable by 1) (--x)

 /* Java Assignment Operators */
 //int x = 5
// = (equal, x = 5)
// += (x += 3)
// -= (x -= 3)
// %= (x %= 3)
// /= (x /= 3)
// &= (x &= 3)
// <<= (x <<= 3 ; x= x << 3)
//>>= (x >>= 3 ; x= x >> 3)
// ^= (x ^= 3; x = x ^ 3)
// |= (x |= 3; x = x|3)

/* Java Comparaison Operators*/

// == (equal to : x==y)
// != (not equal to  x!=y)
// > ( greater than : x>y)
// < (less thanL x<y)
//>= (greater than or equal to)
// <= (less than or equal to)

// Java Logical Operators
// && (logical and ) (Returns true if both statements are true: x<5 && x<10)
// || (logical or) (Returns true if one of the statement is true) (x<5 || x<4)
// ! (logical not) (Reverse the result, returns false if the result is true)
  }
}